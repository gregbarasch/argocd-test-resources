apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: fake-proxy
  namespace: argocd-test
spec:
  virtualhost:
    fqdn: fake.example.com
  routes:
    - conditions:
        - prefix: /
      services:
        - name: non-existent-service  # <-- this service doesn’t exist
          port: 80
